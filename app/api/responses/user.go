package responses

/*func NewUserSignupResponse() *ArticleResponse {
	resp := &UserSignupResponse{}

	if resp.User == nil {
		if user, _ := dbGetUser(resp.UserID); user != nil {
			resp.User = NewUserPayloadResponse(user)
		}
	}

	return resp
}
*/
